# Base URL for the Django REST API.
# Leave as /api to use the Vite dev proxy (see vite.config.ts).
VITE_API_BASE_URL=/api

# Target backend used by the Vite dev proxy.
VITE_PROXY_TARGET=http://localhost:8000
